import type { Capabilities } from "@/lib/types";
export function SettingsClient({ capabilities }: { capabilities: Capabilities }) {
  return <section className="panel p-5"><h2 className="text-lg font-semibold">Runtime configuration (read-only)</h2><p className="my-3 text-sm muted">Values come from the backend. Browser preferences cannot switch inference or verification modes. Deployment settings must be changed on the server.</p><dl className="space-y-4 text-sm"><div><dt className="font-semibold">Configured AI mode</dt><dd>{capabilities.ai_mode}</dd></div><div><dt className="font-semibold">Retrieval</dt><dd>{capabilities.retrieval}</dd></div><div><dt className="font-semibold">Extraction</dt><dd>{capabilities.extraction}</dd></div><div><dt className="font-semibold">External verification</dt><dd>{capabilities.connectors.map(c => <p key={c.name}>{c.name}: {c.mode}</p>)}</dd></div></dl><p className="mt-5 border-t border-line pt-4 text-sm">PASS / FAIL / REVIEW / UNAVAILABLE remain distinct. Unavailable verification requires REVIEW; confidence cannot override a deterministic FAIL. Final decisions require an officer action.</p><p className="mt-3 text-sm muted">This page does not claim live registry access, enabled role-based authentication, or an immutable audit ledger.</p></section>;
}
